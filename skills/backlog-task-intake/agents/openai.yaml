interface:
  display_name: "Backlog Task Intake"
  short_description: "Turn raw backlog items into normalized pending task records"
  default_prompt: "Use $backlog-task-intake to convert backlog intake items into task records safely."
